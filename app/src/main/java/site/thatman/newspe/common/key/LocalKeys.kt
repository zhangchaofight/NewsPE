package site.thatman.newspe.common.key

object LocalKeys {

    //版本记录的key值
    object VERSION {
        //上一个版本
        const val LAST_VERSION = "last_version"

        const val CURRENT_VERSION = "current_version"
        //之前版本记录的list
        const val PREVIOUS_VERSION_LIST = "previous_version_list"
    }
}